#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gnome-session source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gnome-session-49.0-${ARCH:-x86_64}.yspkg" "gnome-session" "49.0" "GNOME session manager"
