#!/bin/sh
set -eu

: "${dir:?set dir to the extracted fontconfig source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-fontconfig-2.16.0-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "fontconfig" "2.16.0" "Font configuration and discovery"
