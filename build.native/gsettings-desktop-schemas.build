#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gsettings-desktop-schemas source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gsettings-desktop-schemas-49.1-${ARCH:-x86_64}.yspkg" "gsettings-desktop-schemas" "49.1" "GNOME desktop settings schemas"
