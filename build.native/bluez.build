#!/bin/sh
set -eu

: "${dir:?set dir to the extracted bluez source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-bluez-5.82-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "bluez" "5.82" "Bluetooth protocol stack"
