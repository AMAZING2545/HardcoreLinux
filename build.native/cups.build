#!/bin/sh
set -eu

: "${dir:?set dir to the extracted cups source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-cups-2.4.11-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "cups" "2.4.11" "Printing system"
