#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gnome-calculator source tree}"
: "${YSPM_BIN:=yspm}"

REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-gnome-calculator-49.0-${ARCH:-x86_64}.yspkg}"

sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "gnome-calculator" "49.0" "GNOME calculator"
