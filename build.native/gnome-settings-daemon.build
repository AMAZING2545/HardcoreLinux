#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gnome-settings-daemon source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gnome-settings-daemon-49.0-${ARCH:-x86_64}.yspkg" "gnome-settings-daemon" "49.0" "GNOME settings and hardware integration"
