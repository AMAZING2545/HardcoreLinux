#!/bin/sh
set -eu

: "${dir:?set dir to the extracted nftables source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-nftables-1.1.4-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-configure" "$dir" "$OUT" "nftables" "1.1.4" "Linux packet filtering framework"
