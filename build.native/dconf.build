#!/bin/sh
set -eu

: "${dir:?set dir to the extracted dconf source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "dconf-0.40.0-${ARCH:-x86_64}.yspkg" "dconf" "0.40.0" "GNOME settings database"
