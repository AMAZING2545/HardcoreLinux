#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gnome-control-center source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gnome-control-center-49.0-${ARCH:-x86_64}.yspkg" "gnome-control-center" "49.0" "GNOME system settings application"
