#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gtk4 source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gtk4-4.20.2-${ARCH:-x86_64}.yspkg" "gtk4" "4.20.2" "GTK 4 widget toolkit"
