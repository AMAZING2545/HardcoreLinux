#!/bin/sh
set -eu

: "${dir:?set dir to the extracted libxml2 source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-libxml2-2.13.8-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "libxml2" "2.13.8" "XML parser library"
