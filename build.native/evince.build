#!/bin/sh
set -eu

: "${dir:?set dir to the extracted evince source tree}"
: "${YSPM_BIN:=yspm}"

REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-evince-48.0-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson"  "$dir" "$OUT" "evince" "48.0" "GNOME document viewer"
