#!/bin/sh
set -eu

: "${dir:?set dir to the extracted gnome-shell source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "gnome-shell-51.0-${ARCH:-x86_64}.yspkg" "gnome-shell" "51.0" "GNOME desktop shell"
