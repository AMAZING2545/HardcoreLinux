#!/bin/sh
set -eu

: "${dir:?set dir to the extracted libepoxy source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-libepoxy-1.5.10-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "libepoxy" "1.5.10" "OpenGL function pointer library"
