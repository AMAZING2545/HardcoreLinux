#!/bin/sh
set -eu

: "${dir:?set dir to the extracted wayland source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-wayland-1.24.0-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "wayland" "1.24.0" "Wayland display protocol libraries"
