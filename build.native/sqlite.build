#!/bin/sh
set -eu

: "${dir:?set dir to the extracted sqlite source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-sqlite-3.50.4-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "sqlite" "3.50.4" "SQLite database engine"
