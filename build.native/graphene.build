#!/bin/sh
set -eu

: "${dir:?set dir to the extracted graphene source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-graphene-1.10.10-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "graphene" "1.10.10" "Graphene vector math library"
