#!/bin/sh
set -eu

: "${dir:?set dir to the extracted upower source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-upower-1.90.8-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "upower" "1.90.8" "Power device abstraction service"
