#!/bin/sh
set -eu

: "${dir:?set dir to the extracted fribidi source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-fribidi-1.0.16-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "fribidi" "1.0.16" "Unicode bidirectional text library"
