#!/bin/sh
set -eu

: "${dir:?set dir to the extracted rfkill source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-rfkill-2.41-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "rfkill" "2.41" "Wireless radio control utility"
