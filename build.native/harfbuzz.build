#!/bin/sh
set -eu

: "${dir:?set dir to the extracted harfbuzz source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-harfbuzz-11.2.1-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "harfbuzz" "11.2.1" "OpenType text shaping engine"
