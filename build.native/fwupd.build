#!/bin/sh
set -eu

: "${dir:?set dir to the extracted fwupd source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-fwupd-2.0.17-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "fwupd" "2.0.17" "Firmware update service"
