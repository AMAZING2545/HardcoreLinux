#!/bin/sh
set -eu

: "${dir:?set dir to the extracted chrony source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-chrony-4.7-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-configure" "$dir" "$OUT" "chrony" "4.7" "Network time synchronization daemon"
