#!/bin/sh
set -eu

: "${dir:?set dir to the extracted pango source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-pango-1.56.4-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "pango" "1.56.4" "Text layout and rendering library"
