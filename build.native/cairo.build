#!/bin/sh
set -eu

: "${dir:?set dir to the extracted cairo source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-cairo-1.18.4-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "cairo" "1.18.4" "2D graphics library"
