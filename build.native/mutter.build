#!/bin/sh
set -eu

: "${dir:?set dir to the extracted mutter source tree}"
: "${YSPM_BIN:=yspm}"

"${REPO_ROOT:-tools}/build-native-meson" "$dir" "mutter-49.0-${ARCH:-x86_64}.yspkg" "mutter" "49.0" "GNOME Wayland compositor and window manager"
