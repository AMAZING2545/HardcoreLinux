#!/bin/sh
set -eu

: "${dir:?set dir to the extracted librsvg source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-librsvg-2.60.0-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "librsvg" "2.60.0" "SVG rendering library"
