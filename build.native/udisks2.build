#!/bin/sh
set -eu

: "${dir:?set dir to the extracted udisks2 source tree}"
: "${YSPM_BIN:=yspm}"

REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-udisks2-2.11.1-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson"  "$dir" "$OUT" "udisks2" "2.11.1" "Storage device management service"
