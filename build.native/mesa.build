#!/bin/sh
set -eu

: "${dir:?set dir to the extracted mesa source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-mesa-51.0-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "mesa" "51.0" "OpenGL, Vulkan and graphics driver stack"
