#!/bin/sh
set -eu

: "${dir:?set dir to the extracted freetype source tree}"
: "${YSPM_BIN:=yspm}"
REPO_ROOT="${REPO_ROOT:-.}"
OUT="${OUTPUT:-freetype-2.13.3-${ARCH:-x86_64}.yspkg}"
sh "$REPO_ROOT/tools/build-native-meson" "$dir" "$OUT" "freetype" "2.13.3" "Font rasterization library"
